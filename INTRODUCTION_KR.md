# agents-devtools 소개

[![Language](https://img.shields.io/badge/INTRODUCTION-English_Ver-blue?style=for-the-badge)](INTRODUCTION.md)

이 문서는 agents-devtools 프로젝트를 처음 접하는 분을 위한 안내서입니다. 설치와 실행 명령 중심의 [README.md](README_KR.md)와 달리, 이 도구가 왜 필요한지에 관한 내용과 내부적으로 어떻게 동작하는지를 설명하는 문서입니다.

## 한 줄 요약

**agents-devtools는 Cloudflare Agents SDK로 만든 에이전트의 진행 상황을 실시간으로 보여주는 로컬 개발 도구(devtools)입니다.**

웹 개발을 할 때 브라우저에서 F12를 눌러 개발자 도구의 Network 탭을 열면, 화면 뒤에서 오가는 요청을 전부 볼 수 있습니다. 이 프로젝트는 그와 같은 경험을 에이전트 개발에 가져오도록 해줍니다. 에이전트가 어떤 함수를 호출받았는지, 상태가 언제 바뀌었는지, 예약된 작업이 성공했는지 실패했는지를 브라우저 화면 하나로 지켜볼 수 있습니다.

## 배경 지식

### Cloudflare Agents SDK란

[Cloudflare Agents SDK](https://github.com/cloudflare/agents)(`agents` npm 패키지)는 Cloudflare Workers 위에서 동작하는 "상태를 가진 에이전트"를 만드는 프레임워크입니다. 일반 서버 함수와 달리 에이전트는 다음과 같은 일을 합니다.

- 자신의 상태(state)를 계속 기억하고 갱신
- 클라이언트와 WebSocket으로 실시간 연결 유지
- "10분 뒤에 이 함수를 실행해줘" 같은 스케줄을 걸어둠
- LLM과 대화하며 스트리밍 응답을 중계
- 외부 도구 서버(MCP)에 연결

**문제는 이 모든 일이 서버 안에서 조용히 일어난다는 점**입니다. `console.log`를 곳곳에 심어 터미널 로그를 눈으로 좇는 것이 사실상 유일한 관찰 수단이었습니다.

### 관측성 이벤트란?

다행히 SDK는 내부에서 중요한 일이 일어날 때마다 구조화된 신호를 방출합니다. 이를 **관측성 이벤트**(observability event)라고 부릅니다. 예를 들면 다음과 같습니다.

| 이벤트 타입                                                    | 언제 발생하는가                         |
|-----------------------------------------------------------|----------------------------------|
| `rpc` / `rpc:error`                                       | 에이전트의 메소드가 호출됐을 때 / 그 호출이 실패했을 때 |
| `state:update`                                            | 에이전트 상태가 바뀌었을 때                  |
| `connect` / `disconnect`                                  | 클라이언트가 WebSocket으로 붙거나 떨어졌을 때    |
| `schedule:create` / `schedule:execute` / `schedule:error` | 예약 작업이 등록/실행/실패했을 때              |
| `chat:recovery:*`                                         | 채팅 스트림이 멈춰 SDK가 자동 복구를 시도할 때     |
| `mcp:client:connect`                                      | 외부 MCP 서버에 연결을 시도했을 때            |

이벤트는 방출되지만, SDK는 이것을 "보여주는" 도구까지 제공하지는 않습니다. 이 프로젝트는 그 빈자리를 채우기 위해 개발되었습니다.

> [!IMPORTANT]
> `agents-devtools`는 비공식 커뮤니티 도구입니다. Cloudflare와 제휴하거나 지원받지 않습니다.

## 전체 구조: 세 개의 조각

이 프로젝트는 세 부분이 릴레이하듯 이벤트를 전달하는 구조를 가집니다.

```
에이전트 안의 client -> (HTTP 배치 전송) -> collector -> (WebSocket 푸시) -> 브라우저 UI
```

### 1. Client - 에이전트 코드 안에 코드 추가

일반적인 경우, 여러분의 에이전트 코드는 TypeScript로 이루어져 있을 것입니다. 여러분의 에이전트 코드에 다음 코드를 추가하세요.

```ts
import { devtools } from "agents-devtools/client";

export class MyAgent extends Agent<Env, State> {
  override observability = devtools();
}
```

`devtools()`는 SDK가 방출하는 이벤트를 가로채지 않고 **복사**합니다. 원래의 방출 동작은 그대로 두고, 사본만 모아뒀다가 250ms마다(또는 50건이 쌓이면) 로컬 collector로 전송합니다.

여기선 핵심적(중요한) 설계 원칙을 사용합니다. **client는 에이전트에 절대 영향을 주지 않습니다.** collector가 꺼져 있으면 전송은 조용히 실패하고, 실패가 반복되면 client는 스스로 비활성화됩니다. 예외를 던지지 않고, 에이전트를 느리게 만들지 않고, 동작을 바꾸지 않습니다. DevTools를 켜든 끄든 에이전트는 똑같이 동작해야 하기 때문입니다.

### 2. Collector - 이벤트를 수집하는 로컬 서버

`npx agents-devtools`로 실행하는 작은 Node 서버입니다. 다음 두 개의 포트를 개방합니다.

- `127.0.0.1:4111` - client가 이벤트를 보내는 수신구(ingest)
- `127.0.0.1:4110` - 브라우저로 여는 UI

받은 이벤트에는 순번(seq)을 붙여 **링 버퍼**(기본 50,000건)에 보관합니다. 링버퍼는 꽉 차면 가장 오래된 것부터 밀어내는 원형 저장소입니다. 밀려난 개수는 드롭 카운트로 집계되어 UI에 표시되므로, 유실이 있었다면 있었다고 제대로 보여줍니다.

보안은 격리가 기본입니다. collector는 `127.0.0.1`(여러분의 로컬 환경)에만 바인딩되어 외부 네트워크에서 접근할 수 없고, 외부로 아무것도 전송하지 않으며, 수신하는 모든 데이터는 형식을 검증하여 이상한 body는 거부합니다.

### 3. UI - 브라우저에서 보는 화면

React로 만든 웹 화면입니다. collector와 WebSocket으로 연결되어 새 이벤트가 도착하는 즉시 화면에 나타나고, 접속 시점 이전의 이벤트도 순번 기준으로 받아와 채웁니다(백필; backfill). 현재 다음의 언어를 지원하며 헤더에서 전환할 수 있습니다.

- 한국어(ko)
- English(en)

## 화면 안내: 다섯 개의 탭

같은 이벤트 스트림을 다섯 가지 관점으로 보여줍니다.

**스트림** - 모든 이벤트를 도착 순서대로 보여주는 기본 화면입니다. 채널 칩으로 종류를 거르고, 텍스트로 검색하고, 행을 클릭하면 payload 전체를 JSON으로 펼쳐 봅니다. 무슨 일이 일어났는지 가장 날것 그대로 보는 곳입니다.

**타임라인** - 왼쪽 사이드바에서 에이전트 인스턴스를 고르면, 그 인스턴스의 실행 단위(fiber 스팬)를 시간 축 위의 막대로 보여줍니다. "언제 무엇이 얼마나 걸렸는지"를 눈으로 파악합니다.

**채팅** - 채팅 에이전트의 턴 목록과 **리커버리 인시던트**(incident, 사고)를 보여줍니다. LLM 스트리밍이 중간에 멈추면 SDK가 자동으로 재시도하는데, 그 감지 -> 시도 -> 예약 -> 성공/실패 과정 전체가 하나의 인시던트 체인으로 묶여 표시됩니다. 개인정보 보호를 위해 대화 내용 자체는 표시하지 않고 메타데이터만 다룹니다.

**스케줄** - 예약 작업을 상태(대기/실행됨/재시도 중/실패/취소)별 카드로 보여주는 보드입니다. 같은 콜백의 예약이 한꺼번에 몰리면 SDK가 내는 중복 경고도 배너로 표시합니다.

**연결** - WebSocket 연결의 수명을 봅니다. 어떤 연결이 언제 붙어 얼마나 유지되다 어떤 코드로 끊겼는지, 지속시간 막대(progress bar)와 함께 표시합니다.

## 시간을 다루는 기능

실시간으로 흘러가는 이벤트는 붙잡아 두고 싶을 때가 있습니다.

- **일시정지 / 재개** - 화면 갱신을 멈추고 현재 상태를 관찰합니다. 재개하면 멈춘 동안 놓친 구간을 collector에서 다시 받아 채우므로 이벤트가 사라지지 않습니다.
- **내보내기** - 지금 보이는 이벤트 전체를 `.ndjson` 파일 하나로 다운로드합니다.
- **재생** - 내보낸(또는 `--record`로 녹화한) `.ndjson` 파일을 화면에 드래그하면 그 세션을 다시 봅니다. "라이브로 복귀"를 누르면 원래 보던 실시간 화면이 그대로 복원됩니다.

이 흐름 덕분에 "버그가 났던 순간"을 파일로 저장해 두고, 나중에 또는 다른 사람 컴퓨터에서 그대로 재현해 볼 수 있습니다.

## 자주 나오는 용어 정리

| 용어           | 뜻                                                                      |
|--------------|------------------------------------------------------------------------|
| NDJSON       | 한 줄에 JSON 하나씩 쌓는 파일 형식. 첫 줄은 세션 정보, 이후 줄은 이벤트 하나씩. 텍스트 편집기로도 열어볼 수 있다. |
| 봉투(envelope) | collector가 이벤트에 순번/수신 시각/채널을 덧씌운 포장. UI와 파일은 항상 이 봉투 단위로 다룬다.          |
| 채널           | 이벤트의 대분류. `agents:rpc`, `agents:schedule`처럼 SDK가 정한 13개 분류가 있다.        |
| 인스턴스         | `에이전트 클래스 이름/개별 이름` 조합. 같은 클래스라도 이름이 다르면 별개의 인스턴스다.                    |
| fiber 스팬     | 에이전트 안에서 하나의 실행 단위가 시작해서 끝날 때까지의 구간                                    |
| 링버퍼          | 용량이 차면 오래된 것부터 밀어내는 저장 방식. collector의 이벤트 보관소                          |
| 리커버리 인시던트    | 채팅 스트림 멈춤 하나에 대한 SDK의 자동 복구 과정 전체를 묶은 단위                               |

## 프로젝트 구조

pnpm 모노레포이며 역할별로 패키지가 나뉩니다.

```
packages/protocol    이벤트 봉투/NDJSON 형식 정의 (의존성 0, 다른 모두가 공유)
packages/client      에이전트에 심는 devtools() (Workers 런타임 대상)
packages/collector   수집 서버 + CLI (npm에 배포되는 유일한 패키지)
packages/ui          React 웹 화면
examples/demo-agent  기능 시연용 데모 에이전트 (API 키 불필요)
```

npm에는 `agents-devtools` 하나만 올라가고, 나머지는 빌드 시점에 그 안으로 병합됩니다.

## 직접 해보기

데모 에이전트가 포함되어 있어 API 키 없이 모든 기능을 체험할 수 있습니다.

```sh
$ pnpm install && pnpm build

# 터미널 1 - DevTools
$ node packages/collector/dist/cli.js

# 터미널 2 - 데모 에이전트
$ cd examples/demo-agent && pnpm dev

# 터미널 3 - 이벤트 발생시키기
$ cd examples/demo-agent && pnpm exercise
```

브라우저에서 `http://127.0.0.1:4110`을 열면 터미널 3의 명령이 만드는 이벤트가 실시간으로 흘러들어오는 것을 볼 수 있습니다. 채팅 리커버리/스케줄 경고/MCP 실패 같은 실전 디버깅 시나리오 재현은 [README.md의 "디버깅 시나리오 재현" 절](README_KR.md#디버깅-시나리오-재현)을 참고하세요.
